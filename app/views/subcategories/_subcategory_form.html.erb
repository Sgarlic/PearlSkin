<%= form_for Subcategory.new do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  添加子类别
  <div class="field">
    <%= f.text_area :name, placeholder: "子类别名字..." %>
  </div>
  <%= f.submit "提交", class: "btn btn-large btn-primary" %>
<% end %>