<% provide(:title, @brand.brand_name) %>

<div class="row">
	<aside class="span1">
		<section>
			<span class="item_brand">
				<%= @brand.brand_name %>
			</span>
			<h5>品牌所属：<%= Country.find(@brand.country_id).country_name %></h5>
		</section>
		<section>
			<% if @brand.brand_addrs.any? %>
				<span class="item_brand">品牌链接 (<%= @brand.brand_addrs.count %>)</span>
				<%= will_paginate @brand_addrs %>
      			<ol class="microposts">
        			<%= render @brand_addrs %>
     			</ol>
      			<%= will_paginate @brand_addrs %>
    		<% end %>
		</section>
		<% if current_user.admin? %>
			<section>
				<%= render 'brand_addr_form' %>
			</section>
		<% end %>
	</aside>

	<div class="span11">
		<% @brand_items = Item.where("brand_id=#{@brand.id}").order('item_english').paginate(page: params[:page]).per_page(10) %>
		<%= render 'shared/item_gridview' , object: @brand_items %>
	</div>

</div>