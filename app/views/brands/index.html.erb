<% provide(:title, '品牌') %>

<div class="row">
	<aside class="span3">
		<section>
			<h1>品牌列表</h1>
		</section>
		<section>
			<%= render 'brand_form' %>
		</section>
	</aside>



	<div class="span8">
		<%= will_paginate @brands, :previous_label => "上一页", :next_label => "下一页" %>
		<ol class="microposts">
	  		<%= render @brands %>
		</ol>
		<%= will_paginate @brands, :previous_label => "上一页", :next_label => "下一页" %>
	</div>

</div>