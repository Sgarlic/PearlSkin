<%= form_for Country.new do |f| %>
	<div class="add_country_form">
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
	  <%= f.text_area :country_name, placeholder: "新的国家..." %>
	</div>
	</div>
  <%= f.submit "提交", class: "btn btn-primary" %>
<% end %>