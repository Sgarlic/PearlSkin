<% provide(:title, '所有用户') %>
<h1>所有用户</h1>

<%= will_paginate, :previous_label => "上一页", :next_label => "下一页" %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= render user %>
    </li>
  <% end %>
</ul>

<%= will_paginate, :previous_label => "上一页", :next_label => "下一页" %>