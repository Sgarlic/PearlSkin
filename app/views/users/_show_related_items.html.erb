
	<ol class="microposts">
		<% @relationships.each do |relationship| %>
			<%= render 'shared/single_item' , object: Item.find(relationship.item_id) %>
  		<% end %>
	</ol>

<%= @relation_items.size %>